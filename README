# 🎉 Question Processor App 🎉

## Overview
Welcome to the **Question Processor App**! This Streamlit app leverages OpenAI's powerful language models to process and extract information from a list of questions. 📚💡

## Features 🚀
- 📄 **Upload a text file** containing multiple questions.
- 🧠 **Process each question** to extract structured data: query type, the question itself, and a concise answer.
- 📊 **Display results** in a user-friendly JSON format.

## Requirements 🛠️
To get started with this application, ensure you have:

- Python 3.7 or higher 🐍
- Required Python packages: Streamlit, LangChain, OpenAI API Client, Pydantic, and Python-Dotenv

Install the required packages with:
```bash
pip install streamlit langchain openai pydantic python-dotenv
